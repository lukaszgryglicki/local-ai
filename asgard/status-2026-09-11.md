# asgard local-AI — status 2026-09-11 (end of groundwork, before model downloads)

Verbatim final report of the groundwork session (kept here so the state of play is in the repo,
not only in a chat log). Everything below was true at ~20:00 CEST 2026-09-11; what happened
next (T0 downloads + tests) is in `asgard/results-t0.md`.

---

**Final status — asgard local-AI groundwork (everything except model downloads, which stay gated on you)**

Quadro RTX 5000 under Vulkan/FreeBSD: works. `test-backend-ops -b Vulkan0`: 0 FAIL in ≈ 19 200
cases (111 ops 16 214/16 214, fused ops 2 153/2 153, MUL_MAT_ID 883/883, quantized
MUL_MAT_ID_FUSION 6/6). The only abort is the FreeBSD-nvidia 256 MiB single pinned-allocation cap
(probe `asgard/vkalloc.c`; VRAM and total pinned unaffected) → rules: `--load-mode none`, never
`--check-tensors`, `--cache-ram 0` for 9B/Gemma-class KV; optional ~20-line staging-chunk patch
noted (plan §6 rule 6).

Research merged (two wide sweeps, ~20 labs, sizes re-fetched from HF):

- T0 (all in VRAM): North-Mini-Code-1.0 (Cohere 30B-A3B coder, thinking, 500K native, SWE-V 67.6)
  UD-IQ3_XXS 10.9 GiB → 15.7 GiB @262K (98 %, `--n-cpu-moe 1–2` if OOM), ~100–130 t/s; fork already
  has `cohere2moe` + its chat parser. Tied embeddings verified from the GGUF header (+0.41 GiB on
  GPU). T0 safe: Qwen3.5-9B Q8_0. T0 alt: Gemma-4-26B-A4B (LCB 77 but SWE-V 57).
- T1: Qwen3.6-35B-A3B non-MTP UD-IQ3_S `--n-cpu-moe 5` (alt MTP UD-IQ3_XXS k=7). T1b:
  KAT-Coder-V2.5-Dev (coding fine-tune, +5 SWE-V, no MTP head). T2/T3 unchanged.
- Config trap found: `--spec-type draft-mtp` on a model without MTP layers makes llama-server exit →
  `ngram-mod` only for North/KAT/Gemma/non-MTP files.

Docs shipped to asgard `/data/local-ai` (uncommitted — you commit; they now show as staged, I did not
`git add`): `asgard/plan.md`, `asgard/research/{qwen,non-qwen,llamacpp-vulkan,sweep-cn,sweep-west}.md`
+ `test-backend-ops-2026-09-11.txt`, `asgard/vkalloc.c`, `readme.md` (asgard section + Files row).
tuxi checkout untouched. Batch3 README got the evening addendum (5 GHz closed, dataset swap,
clang-21 trap, gate, rules) and was rsynced to asgard.

Housekeeping: epp back to 100, ACPI debug sysctls back to 0, temp files removed (kept
`/var/tmp/llama-build-vulkan2.log` and `suspend-test/` — both referenced by docs), no test/server
processes left.

Open (your call): BIOS Turbo, `config/poc.env` `BUILD_JOBS=6` (tuxi value), VRAM survival across S3
untested (stop server before `zzz`), dev-tree extraneous files, the pinned-cap driver-source check if
you want certainty.

---

## Addendum, same evening (20:00–20:30) — pre-push secrets check, downloads un-gated

- `/data/local-ai` secrets triple-check before pushing (repo is public:
  github.com/lukaszgryglicki/local-ai, 21 commits): (1) the exact contents of `key.secret`,
  `remote-host.secret`, `remote-key.secret` occur in **0 revisions and 0 tracked files**
  (`git grep -F` over `git rev-list --all`); (2) no HF/GitHub/OpenAI/AWS/Slack/Google token patterns,
  no `BEGIN … PRIVATE`, no JWTs in the worktree or in `git log --all -p`; (3) no public IPv4, MAC or
  e-mail addresses anywhere (the only IPs are 10.253.254.1 / 127.0.0.1; the "1.4.357.0"/"595.99.2.0"
  hits in plan.md are Vulkan/driver version strings); every script reads credentials from the
  `*.secret` files (`tunnel.sh`, `copilot*.sh`, `qwen*.sh`, `health*.sh`, `remote/bench-ab.py`).
  Fixed one gap: `models/` and `*.part` added to `.gitignore` (`*.gguf` did not match the
  in-progress `.gguf.part` download, so `models/` showed up as untracked).
- Downloads un-gated: T0 models go to `/data/local-ai/models/` (gitignored, on the
  `zroot/data/local-ai` dataset: compression=off, primarycache=metadata, recordsize=128K) one at a
  time via `asgard/download.sh MODEL` (resume + size + sha256 from `asgard/models.sh`), each followed
  by the full test (`asgard/serve.sh` + `asgard/rust-test.sh`) before the next download starts.
- asgard variants of the scripts (like the local / remote (linode) / rpc variants before):
  `asgard/models.sh` (per-model file, repo rev, sha256, spec-type, cache-ram, sampling, thinking
  toggle), `asgard/serve.sh MODEL`, `asgard/download.sh MODEL`, `asgard/health.sh`,
  `asgard/qwen.sh` (MODEL=…), `asgard/bench.py`, `asgard/rust-test.sh MODEL`.

---
*Postscript 2026-09-12 08:1x*: the S3 question above is answered — a server with GPU work
in flight hangs forever after a resume (fence wait). Settled in `asgard/ops.md`: the owner stops/starts llama around
his own `zzz`; only the thermal watchdog's suspend action stops it first and restarts it after; `sudo service llama
start|stop|restart|status` is a stub over `asgard/llamactl.sh`. Details: `asgard/results-t0.md` ("S3 live test").
